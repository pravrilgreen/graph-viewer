{
  "name": "transition-map-builder",
  "version": "1.0.0",
  "description": "Transition Map Builder",
  "author": "Your Organization",
  "license": "Proprietary",
  "private": true,
  "scripts": {
    "start:backend": "cd backend && python -m venv venv && source venv/bin/activate && pip install -r requirements.txt && uvicorn main:app --reload",
    "start:frontend": "cd frontend && npm install && npm run dev",
    "start:all": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "seed": "cd backend && python seed.py"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}
